# Customer-Type-Finder
This project classifies customers into budget levels:  Low  Normal  High  Using KNN (K-Nearest Neighbors), the model predicts customer category based on purchasing behavior and spending patterns.
